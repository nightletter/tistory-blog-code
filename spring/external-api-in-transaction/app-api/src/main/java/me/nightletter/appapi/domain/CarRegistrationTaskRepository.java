package me.nightletter.appapi.domain;

import org.springframework.data.jpa.repository.JpaRepository;

public interface CarRegistrationTaskRepository extends JpaRepository<CarRegistrationTask, String> {
}
